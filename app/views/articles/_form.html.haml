= simple_form_for @article do |f|
	= f.collection_select :category_id, Category.all, :id, :name, {prompt: "Please choose a category"}
	= f.input :title
	= f.input :content
	= f.input :prepared_by
	= f.submit