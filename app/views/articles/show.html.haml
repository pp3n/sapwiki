%h1 This is the place holder for show page.

%h1= @article.title
%p= @article.content
%p= @article.prepared_by

= link_to "Back", root_path
= link_to "Edit", edit_article_path(@article)
= link_to "Delete", article_path(@article), method: :delete, data: {confirm: "Are you sure?"}